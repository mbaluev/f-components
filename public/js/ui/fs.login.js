$(function(){
    document.title = '[ПМ Форсайт] Вход в систему';

    // form validate
    var validator = $('#form-login').kendoValidator().data('kendoValidator');

    // login button
    $("#login").kendoButton();
});
